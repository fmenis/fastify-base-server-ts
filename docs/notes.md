# Notes

### Prisma

- npx prisma init: creates prisma dir and add an entry inside .env file
- compile the .env new entry
- compile the models
- npx prisma migrate dev --name {{name}}: generate and apply the migration
- npx prisma format
